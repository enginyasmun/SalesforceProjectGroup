import os
import sys

os.environ["SECRET_KEY"] = "REPLACE_WITH_A_LONG_RANDOM_SECRET"
os.environ["ADMIN_NAME"] = "Academy Administrator"
os.environ["ADMIN_EMAIL"] = "YOUR_ADMIN_EMAIL"
os.environ["ADMIN_PASSWORD"] = "YOUR_STRONG_ADMIN_PASSWORD"
os.environ["PROJECT_GROUP_DATABASE"] = "/home/enginproject/project-group-data/project_group.db"
os.environ["PROJECT_GROUP_UPLOADS"] = "/home/enginproject/project-group-data/uploads"
os.environ["PROJECT_GROUP_AVATARS"] = "/home/enginproject/project-group-data/avatars"
os.environ["APP_BASE_URL"] = "https://enginproject.pythonanywhere.com"
os.environ["COOKIE_SECURE"] = "1"

# Optional Gmail SMTP email delivery. Use a Google App Password.
os.environ["EMAIL_ENABLED"] = "0"
os.environ["SMTP_HOST"] = "smtp.gmail.com"
os.environ["SMTP_PORT"] = "587"
os.environ["SMTP_USE_TLS"] = "1"
os.environ["SMTP_USERNAME"] = "YOUR_GMAIL_ADDRESS"
os.environ["SMTP_PASSWORD"] = "YOUR_GOOGLE_APP_PASSWORD"
os.environ["EMAIL_FROM"] = "YOUR_GMAIL_ADDRESS"

project_path = "/home/enginproject/SalesforceProjectGroup"
if project_path not in sys.path:
    sys.path.insert(0, project_path)

from app import app as application
